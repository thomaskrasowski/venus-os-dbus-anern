# Driver source

dbus-anern.py is the owner's locally supplied baseline, not the earlier compact
reconstruction. See [provenance](../docs/SOURCE-STATUS.md) and
[limitations](../docs/known-issues.md). No remote deployment is part of this import.
The driver uses Venus OS runtime dependencies and is not intended to run on Windows.
